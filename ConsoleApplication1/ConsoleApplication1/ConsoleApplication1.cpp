#include <iostream>
#include<math.h>
using namespace std;
int main()
{
    float a, b, c, d;
cin >> a >> b >> c >> d;
cout << "tong" << (a / b) + (c / d);
cout << "tich" << (a / b) * (c / d);
cout << "thuong" << (a / b) / (c / d);
cout << "hieu" << (a / b) - (c / d);
    return 0;
}
